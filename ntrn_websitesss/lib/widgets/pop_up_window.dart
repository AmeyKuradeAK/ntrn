import 'package:flutter/material.dart';

class PopUpWindow extends StatelessWidget {
  const PopUpWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('PopUpWindow Widget'),
    );
  }
}
