import 'package:flutter/material.dart';

class IntroSequence extends StatelessWidget {
  const IntroSequence({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('IntroSequence Widget'),
    );
  }
}
